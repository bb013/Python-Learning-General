# This is sorta a scratch pad that I use to test and learn whatever code I'm trying to understan. It changes and gets rewritten

#word =input("enter something")
#i = word.lower()
#print(i)

#stringinput = "random string text"
#print(type(stringinput))# outputs clas type
#print(dir(stringinput))# 'dir' command tell us what kind of operations can be performed on the variable stringinput

#how to open a file:
handle = open(filename, mode)

#mode is optional and should be 'r' if we ar planning to read the file and 'w' if we are going to write to the file